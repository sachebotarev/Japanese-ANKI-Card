# Japanese ANKI Card

Репозиторий для подготовки карточек Anki при изучении японского языка. Слева по данным пайплайна слова из `Слова/` превращаются в JSON, иллюстрации и озвучку, затем попадают в колоду **«Японские слова»** и в наблюдаемые заметки **Obsidian (`Записи/`)**; при сборке статического сайта тот же контент уходит на **GitHub Pages (Quartz)**.

Все переводы по карточкам и учебный текст заметок ведутся **на русском**.

## Структура каталогов

| Путь | Назначение |
|------|------------|
| `Слова/` | Списки лексики: один файл на тематику (или логический список), по одному слову на строку; перед правками файл нормализуют [по правилам](docs/word-lists.md) |
| `Карточки/<тема>/` | Один JSON на слово — источник правды для колоды и связанных артефактов |
| `Картинки/<тема>/` | Локальные изображения (имя базы как у JSON) |
| `Произношение/<тема>/` | Локальные MP3 к карточке: `{Слово}_слово.mp3`, `{Слово}_пример_{1–3}.mp3` для нового формата поля `Пример` |
| `Записи/<тема>/` | Заметки Markdown для Obsidian (и копируемый контент для Quartz): одна тема = одна подпапка, имя файла = кандзи/слову из карточки |
| `quartz_overlay/` | Наш оверлей поверх ванильного Quartz: `quartz.config.ts`, `quartz.layout.ts`, `MyGraph` и его inline‑скрипт. Сам Quartz клонируется на сборку в `.quartz-build/` (см. [docs/quartz-site.md](docs/quartz-site.md)) |
| `scripts/` | Утилиты: озвучка, импорт картинок, теги Minna, синк в Anki и др. — см. [scripts/README.md](scripts/README.md) |
| `docs/` | Детальные спецификации и обходные сценарии |
| `AGENTS.md` | Краткая инструкция для ИИ-агента в этом репозитории |

Имя **темы** должно совпадать у `Слова/<тема>`, `Карточки/<тема>/`, `Картинки/<тема>/`, `Произношение/<тема>/` и `Записи/<тема>/` (где применимо).

## Пайплайн по шагам

1. **Слова** — убрать пустые строки и дубли, отсортировать строки файла темы.

2. **Тематика без явного указания** — поместить слово в наиболее подходящий уже существующий файл `Слова/` и дерево `Карточки/`. Для существительных искать тему применения, а не класть всё подряд в `Существительные`; эта тема только запасной вариант. Новую тему заводят лишь при осмысленной малой группе слов и отсутствии лучшего ящика среди уже существующих.

3. **JSON-карточка** по [docs/cards-spec.md](docs/cards-spec.md): в полях **`Пример`**, **`Пример без слова`**, **`Пример перевод`** — ровно **три** фрагмента, через тег `<br>` (единый текстовый формат). От этого зависят скрипт озвучки и структура заметки.

4. **Теги** — обязательные части речи/JLPT/темы плюс, по правилам проекта: для лексики из *Minna no Nihongo* — тег урока **`みんな初級I-第NN課`** или **`みんな初級II-第NN課`**; для остального — **`みんな-教材外`**. Полный регламент в разделе «Теги» в [cards-spec.md](docs/cards-spec.md). Массовая простановка/правка локального словаря уроков: `scripts/apply_minna_lesson_tags.py`.

5. **Картинка** — если в JSON поле **`Картинка`** текстовый промпт и есть техническая возможность генерации, иллюстрацию создавать и класть в `Картинки/<тема>/`; полноценный raster-снимок по смыслу третьего примера, не заглушка.

6. **Озвучка** — через локальный **AivisSpeech** и **`scripts/generate_aivis_audio.py`**: допускаются только голоса из белого списка, скорость по умолчанию 90 %, случайный выбор голоса **на каждый** из четырёх MP3, если не задан `--speaker`. Детали: [docs/audio.md](docs/audio.md). Карточки без трёх примеров по `<br>` скриптом не поддерживаются; массовое обновление валидных карточек: `scripts/regenerate_audio_all_valid.py`.

7. **Записи (Obsidian)** — при создании новой карточки файл `Записи/<тема>/<Слово>.md` по [docs/zapisi-spec.md](docs/zapisi-spec.md) (раздел примеров **без** строки из «Примера с пропусками» для Anki — там только полный пример и перевод). Обновление только YAML-блока `tags:` из JSON при необходимости: `scripts/sync_zapisi_tags_from_cards.py`. Массовая генерация из JSON: `scripts/generate_zapisi_from_cards.py` (ускорены карточки с полным набором картинок и аудио).

8. **Сайт (Quartz)** — подготовка контента под Pages: **`python3 scripts/sync_quartz_content.py`**, сборка ванильным Quartz во временный клон `.quartz-build/` поверх оверлея `quartz_overlay/`; обзор: [docs/quartz-site.md](docs/quartz-site.md). Теги в frontmatter ключом **`tags:`** подхватываются как теги сайта для фильтрации.

9. **Anki** — поля заметки из JSON и медиафайлы; MCP `anki` или массовый сценарий **AnkiConnect**: `scripts/sync_json_to_anki_connect.py` (см. [docs/anki-sync.md](docs/anki-sync.md)). По правилам обновления читите тот же документ — часть случаев **не перетирает** озвучку в колоде, если пример совпадает.

10. **Git** — после появления **новой** карточки и артефактов обычно делаются коммит и push в `main`, **если** пользователь отдельно не запретил публикацию.

## Документация

| Документ | Содержание |
|----------|------------|
| [docs/cards-spec.md](docs/cards-spec.md) | Поля JSON, формат примеров, теги (включая Minna и Quartz-friendly тег урока) |
| [docs/anki-sync.md](docs/anki-sync.md) | Обновление и обратная синхронизация; AnkiConnect-скрипт |
| [docs/audio.md](docs/audio.md) | AivisSpeech, белый список голосов, четыре файла озвучки |
| [docs/zapisi-spec.md](docs/zapisi-spec.md) | Структура заметки в `Записи/` |
| [docs/quartz-site.md](docs/quartz-site.md) | Сайт на GitHub Pages, теги, raw-медиа |
| [docs/word-lists.md](docs/word-lists.md) | Работа с файлами в `Слова/` |
| [scripts/README.md](scripts/README.md) | Командная строка к основным скриптам |

## Короткие запросы в чат агента

Фразы вроде **«Добавь слово 口座»** или **«Создай карточку для 口座»** по умолчанию трактуются как **полный цикл**, если не указано обратное: тема → `Слова/` → JSON → картинка → озвучка → `Записи/` (если возможно) → обновление Anki → при новой карточке коммит/push без отдельного запрета.

Ограничить объём можно явно, например:

- `Только JSON для слова 口座`
- `Без Anki`
- явный запрет на коммиты/push для этой сессии.

## Окружение

- **Anki**, колода `Японские слова`, тип заметки `Японские слова`.
- Локально: **AivisSpeech** (+ **ffmpeg**) для генерации озвучки.
- По желанию: дополнение **AnkiConnect** для `scripts/sync_json_to_anki_connect.py`; **MCP `anki`** — для операций через агента.
- Сайт: **Node ≥ 22**, **npm ≥ 10.9** (требование апстрима Quartz), см. [docs/quartz-site.md](docs/quartz-site.md).
